{
  "name": "utraty",
  "version": "1.0.0",
  "private": true,
  "description": "Osobní evidence útrat po výplatních obdobích — PWA pro iPhone.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "serve": "python3 -m http.server 8080 --directory app"
  }
}
